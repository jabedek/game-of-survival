import { DataCellDirective } from './data-cell.directive';

describe('DataCellDirective', () => {
  it('should create an instance', () => {
    const directive = new DataCellDirective();
    expect(directive).toBeTruthy();
  });
});
